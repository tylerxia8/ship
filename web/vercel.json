{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd .. && pnpm install --frozen-lockfile --filter @ship/web --filter @ship/shared && pnpm --filter @ship/shared build && pnpm --filter @ship/web build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
