{
  "name": "@ship/cli",
  "version": "0.0.0",
  "private": true,
  "description": "Plugforge reference CLI integration for Ship",
  "type": "module",
  "bin": {
    "ship": "./src/index.mjs"
  },
  "scripts": {
    "build": "node src/index.mjs --help",
    "smoke": "node src/index.mjs --help",
    "test:drill": "corepack pnpm --filter @ship/sdk build && vitest run tests/ttfe.drill.test.ts",
    "type-check": "node src/index.mjs --help"
  },
  "dependencies": {
    "@ship/sdk": "workspace:*"
  }
}
