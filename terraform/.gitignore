# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.secret
.terraform.lock.hcl

# Exception: the ShipShape Vercel+Render+Neon module commits its lock file
# (pins verified provider versions after the 2026-05-22 init + validate run).
!render-vercel-neon/.terraform.lock.hcl

# Crash logs
crash.log
crash.*.log

# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Sensitive variable files
*.auto.tfvars.secret
